---
name: "Check syntax python"
description: "test"
inputs:
  shell_ex:
    description: "test"
    required: true
    default: "ls -l ."
runs:
  using: "composite"
  steps:
    - run: ${{ github.action_path }}/test/script.sh
      shell: bash ${{ inputs.shell_ex }}
