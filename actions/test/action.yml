---
name: "Check syntax python"
description: "test"
inputs:
  shell_ex:
    description: "test"
    required: true
    default: "ls -l ."
runs:
  using: "composite"
  steps:
    - run: bash -c "${{ inputs.shell_ex }}"
