name: Build

on:
  push:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - run: ls -l
      - name: Checkout
        run: |
         echo "hello" > test.txt 
      - uses: .github/test.yml
      - uses: .github/workflows/test.yml
